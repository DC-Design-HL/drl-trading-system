# DRL Trading System — Training Dependencies (Mac M3 Pro)
# Install: pip install -r requirements-training.txt

# PyTorch with MPS (Metal) support for Apple Silicon
# DO NOT use --extra-index-url cpu — Mac M3 has GPU acceleration via MPS
torch>=2.1.0
torchvision>=0.16.0

# Core RL
stable-baselines3[extra]>=2.3.0
sb3-contrib>=2.3.0
shimmy>=1.3.0
gymnasium>=0.29.1

# Data Processing
pandas>=2.1.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Technical Analysis (training features)
# ta-lib optional — install via brew if available

# Visualization (optional, for training plots)
matplotlib>=3.7.0
seaborn>=0.12.0

# Utilities
psutil>=5.9.0
pyyaml>=6.0.1
requests>=2.31.0
tqdm>=4.65.0

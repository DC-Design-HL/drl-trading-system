# 2026-03-22 Session Notes

## Major Changes Today

### ETH Model Trained and Deployed
- ETH walk-forward training completed: 9/9 folds, avg Sharpe 6.58, avg return +19.8%/2mo, 100% positive folds, 0 overfit flags
- Best fold: #8 (Sharpe 9.90, +34.2% return)
- Significantly stronger than BTC model (Sharpe 6.58 vs 2.70)
- `live_trading_htf.py` updated: `--symbol` flag, `HTF_SYMBOL` env var, symbol-aware model finder (`_find_best_fold_model` helper)
- WebSocket stream made symbol-aware (uses `{symbol.lower()}@aggTrade`)
- systemd service `drl-htf-eth` created at `/etc/systemd/system/drl-htf-eth.service`
- Models at `data/models/htf_walkforward_eth/`

### Futures Testnet Migration (from Spot)
- **BinanceFuturesConnector** (`src/api/binance_futures.py`): Direct REST API, HMAC signing, no ccxt
- **FuturesTestnetExecutor** (`src/api/futures_executor.py`): Real LONG/SHORT, SL/TP orders, position sync
- API server updated: all `/api/testnet/*` endpoints pull from futures exchange
- TestnetExecutor delegates to FuturesTestnetExecutor
- `_mirror_testnet()` skips CLOSE actions — exchange handles exits autonomously
- Credentials: `BINANCE_FUTURES_API_KEY`, `BINANCE_FUTURES_API_SECRET`, `BINANCE_FUTURES_BASE_URL=https://demo-fapi.binance.com`
- 108 tests passing

### Demo-fapi Testnet Limitations (CRITICAL)
- demo-fapi does NOT support: STOP_MARKET, TAKE_PROFIT_MARKET, STOP, TAKE_PROFIT, TRAILING_STOP_MARKET (all return error -4120)
- Only MARKET and LIMIT orders work
- **TP workaround**: LIMIT order with `reduceOnly=true` at TP price → fills autonomously
- **SL workaround**: Bot WebSocket monitors mark price, fires MARKET close when SL hit
- Comprehensive skill created: `skills/binance-futures-api/SKILL.md` (841 lines)

### Bug Fixes
- **testnet_executor mirror bug**: `mirror_trade()` looked for `price` key but `_close_position()` uses `exit_price` → close never mirrored. Fixed fallback.
- **Live Portfolio tab bug**: `api_server.py` had "MATHEMATICAL OVERRIDE" summing ALL historical trade log PnL ($4.5M accumulated). Fixed to use state file balances.
- **TP sync on restart**: Added `_sync_order_tracking()` and `ensure_tp_order()` for idempotent TP management

### BTC Bot Activity
- SHORT from Mar 21 ($70,616 entry) closed via TP at $68,472 → +$149.80 PnL
- WebSocket caught the TP properly

## Current State (as of 19:17 UTC)
- **Bots: STOPPED** — drl-htf-agent and drl-htf-eth are OFF, waiting for Chen to say start
- **Exchange: FLAT** — 0 positions, 0 orders, wallet $4,989.96
- **SL/TP enforcement: DONE** — if TP placement fails, position auto-closes. 10 E2E integration tests pass against real demo-fapi.
- **Tick size bug fixed** — BTCUSDT prices now rounded to tick size (0.10) to avoid -4014 errors
- **All trade logs cleared** — clean slate
- **Crons DISABLED** — trade alert checker + hourly task check both off to save tokens
- **Anthropic API key expired** — Claude Code via bash script no longer works. Must use `sessions_spawn` with `runtime="subagent"` (OpenClaw routing) instead.
- **108 unit tests + 10 E2E integration tests = all passing**

## Skills Created Today
- `testnet-autonomous-exits` — How exits should work via exchange orders
- `testnet-real-portfolio` — All data from exchange, zero local calcs
- `binance-futures-api` — Complete API reference (841 lines), verified against demo-fapi
- `binance-futures-testnet` — Architecture overview
- `testnet-validation-checklist` — 3 critical requirements validation

## Key Preferences Reinforced by Chen
1. Claude Code does ALL research + implementation — CEO should NOT guess at API behavior or write code fixes
2. NEVER open a position without SL and TP — non-negotiable
3. All testnet data from exchange API — zero local calculations
4. How to run Claude Code: pipe task from file to `claude -p --permission-mode bypassPermissions` as claudeuser
5. When Claude Code bash fails, use `sessions_spawn` with `runtime="subagent"` as fallback
6. **NO AI CRONS** — cron jobs that fire into AI session waste massive tokens (~230K cached per invocation). All disabled. Trade alerts/monitoring should be system-level scripts if needed.
7. **Compaction threshold: 300K tokens** — alert Chen to compact when we reach this

## Latest Commits
- `12c4974` — SL/TP enforcement + E2E integration tests + tick size rounding fix

## Pending Actions (as of 19:20 UTC)
- Chen needs to say "start bots" — they're stopped, SL/TP enforcement is ready
- Consider setting up system-level bash cron for trade alerts (no AI involvement)
- Chen asked about reducing token usage — explained cron was the main culprit, now fixed
